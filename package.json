{
  "name": "ecs-wait-action",
  "version": "1.0.0",
  "main": "src/index.ts",
  "author": "oryan@moshe.ai",
  "license": "ISC",
  "scripts": {
    "build": "esbuild --bundle --minify --platform=node src/index.ts --outfile=dist/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@aws-sdk/client-ecs": "^3.797.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3"
  }
}
